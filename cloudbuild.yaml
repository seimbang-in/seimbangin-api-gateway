options:
  logging: CLOUD_LOGGING_ONLY

steps:
  - name: "gcr.io/cloud-builders/npm"
    entrypoint: "bash"
    args: ["-c", "npm install && npm run build"]

  - name: "gcr.io/cloud-builders/docker"
    args:
      - "build"
      - "-t"
      - "gcr.io/$PROJECT_ID/seimbangin-api"
      - "."

  - name: "gcr.io/cloud-builders/docker"
    args:
      - "push"
      - "gcr.io/$PROJECT_ID/seimbangin-api"

  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: "gcloud"
    args:
      - "run"
      - "deploy"
      - "seimbangin-api"
      - "--image"
      - "gcr.io/$PROJECT_ID/seimbangin-api"
      - "--platform"
      - "managed"
      - "--region"
      - "asia-southeast2"
      - "--allow-unauthenticated"
      - "--set-secrets=DB_CONNECTION=seimbangin-api-env:latest"
      - "--set-secrets=DB_HOST=seimbangin-api-env:latest"
      - "--set-secrets=DB_PORT=seimbangin-api-env:latest"
      - "--set-secrets=DB_NAME=seimbangin-api-env:latest"
      - "--set-secrets=DB_USER=seimbangin-api-env:latest"
      - "--set-secrets=DB_PASSWORD=seimbangin-api-env:latest"
      - "--set-secrets=GOOGLE_CLOUD_CREDENTIALS=seimbangin-api-env:latest"
      - "--set-secrets=BUCKET_NAME=seimbangin-api-env:latest"
      - "--set-secrets=JWT_SECRET=seimbangin-api-env:latest"
      - "--quiet"

images:
  - "gcr.io/$PROJECT_ID/seimbangin-api"
